cmake_minimum_required(VERSION 3.16)

SET(externalFiles
    
)
SET(includeFiles
    
)
SET(libraries
    
)

DUCKVIL_MODULE("RuntimeCompiler" "SHARED" "${externalFiles}" "${includeFiles}" "${libraries}")