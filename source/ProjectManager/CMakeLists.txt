cmake_minimum_required(VERSION 3.16)

SET(externalFiles
    
)
SET(includeFiles
    
)
SET(libraries
    SDL2
)

DUCKVIL_MODULE("ProjectManager" "SHARED" "${externalFiles}" "${includeFiles}" "${libraries}")