#include "Utils/UUID.h"

namespace Duckvil {

    // static UUIDv4::UUIDGenerator<std::mt19937_64> g_GeneratorUUID;

    // UUID::UUID() :
    //     m_uuid(g_GeneratorUUID.getUUID()),
    //     m_ucSize(UUID::SIZE),
    //     m_ucVersion(static_cast<uint8_t>(UUID::VERSION))
    // {

    // }

    // UUID::~UUID()
    // {

    // }

}